# EVE 未完成项清单

生成时间：2026-07-25

## 未完成项

### 1. speak.py REAL 模式 — TTS 后端未接入

- **位置**: eve/output/speak.py `_execute_real()`
- **缺失内容**: 真实的 TTS 语音合成调用（pyttsx3 / win32com / edge-tts）
- **原因**: 当前阶段未决定具体 TTS 方案，接口已就绪
- **是否影响主链路**: 否（语音输出非当前核心功能）
- **下一步**: 选择本地 TTS 方案后补入

### 2. LLMAdapter 未在真实模型上验证加载和推理

- **位置**: eve/core/model_adapter.py LLMAdapter.load() / infer()
- **缺失内容**: 使用真实 DeepSeek/Qwen 模型文件进行端到端推理测试
- **原因**: 本地 `eve/core/deepseek-7b/` 和 `eve/core/qwen/` 目录存在但是否有完整模型文件未确认
- **是否影响主链路**: 轻度影响 — LLM 是大循环的核心，但系统其他模块（TNN/Graph/Memory/Dock）可独立运行
- **下一步**: 确认本地模型文件完整性后加载测试

### 3. VLMAdapter / YOLOAdapter 未在真实模型上验证

- **位置**: eve/core/model_adapter.py
- **缺失内容**: 真实 VLM/YOLO 模型加载和推理
- **原因**: 本地 yolo26 目录存在但未验证加载
- **是否影响主链路**: 否（视觉能力可以后续接入，不影响系统骨架）
- **下一步**: 补充视觉模型验证

### 4. 音频捕获未实现

- **位置**: eve/input/（缺少 audio_capture.py）
- **缺失内容**: 麦克风音频捕获线程
- **原因**: 音频输入不是当前阶段核心需求
- **是否影响主链路**: 否
- **下一步**: 按需补充

### 5. 云端 LLM 异步调用未实现

- **位置**: eve/core/loop.py（缺少云端 LLM 循环）
- **缺失内容**: 异步提交困难问题给云端 LLM 的循环
- **原因**: 需要先验证本地 LLM 基本可用后再接云端
- **是否影响主链路**: 否（设计为辅助增强，非必需）
- **下一步**: 本地 LLM 验证后补充

### 6. 资源监控循环未实现

- **位置**: eve/core/loop.py（缺少资源监控循环）
- **缺失内容**: CPU/GPU/内存/显存实时监控
- **原因**: 监控能力不影响核心逻辑，可后续补充
- **是否影响主链路**: 否
- **下一步**: 按需补充

### 7. 三阶段发育实验未执行

- **位置**: 整体
- **缺失内容**: 红圆→红圆+蓝三角→文本条件 的完整实验
- **原因**: 这是代码完成后的研究验证阶段，不是本轮代码实现目标
- **是否影响主链路**: 否（实验接口已通过 input/output/Memory/Dock 就绪）
- **下一步**: 代码稳定后执行实验

## 确认不需要实现的内容

以下内容在当前阶段明确不需要实现：

- 云端 LLM 自动调用触发规则（应由实际运行经验形成）
- 激素映射函数的精确参数（需运行数据调优）
- TNN 自动拆分/合并（当前阶段提供手动接口即可）
- 复杂记忆遗忘策略（基础删除接口已实现）
- 商业级 GUI（当前 tkinter 面板功能完整）
- 多游戏/多场景适配（当前聚焦单场景验证）
