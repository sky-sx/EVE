# EVE 路线图与未决问题

本文件只保留当前仍有效的实现顺序和待实验问题。目标语义见 `ARCHITECTURE.md`，代码事实见 `STATUS.md`。

## 2026-07-28 当前下一步（取代旧的最高优先级）

本轮 GUI 与正式交互运行时主干已接通。接下来不再继续扩张框架，优先补齐缺少真实外部条件的验收：

1. 用户提供或选择一个可本地加载的生成式 LLM 路径和一个 VLM 路径；两者必须支持 CUDA 4-bit NF4，禁止静默全精度回退。
2. 在桌面会话中完成 control 的 24 步人工验收，尤其是物理 Esc、原子授权后的真实鼠标/键盘/Unicode/TTS，以及关闭后的进程/共享内存检查。
3. 为实际模型记录首 token/总延迟、显存占用、取消行为、错误恢复和长期稳定性；没有权重时不填写伪造结果。
4. 用真实已存在的五个 TNN artifact 做五槽同时加载与资源压力验收；自动测试已经验证纯运行时五槽上限和第六个拒绝语义。
5. 确认当前最小 Event/Memory 整理确实服务实际交互后，再决定是否增加任何图关系；不预建 Memory Graph。

4-bit 本地模型策略已经确定，不再作为未决问题：

```text
CUDA required
BitsAndBytesConfig(load_in_4bit=True)
NF4 + double quantization
BF16 compute
verify is_loaded_in_4bit
failure => visible error, never FP16/FP32 fallback
```

以下旧路线保留为历史背景，其中 GUI、键盘活动/活动窗口、Event、LLM/VLM 队列、云端接口、六个最小激素和受控真实 Output 已在当前阶段实现；音频输入明确不在本阶段范围。

## 1. 当前最高优先级：修复运行语义漂移

在继续扩展 Dock 或模型能力前，先完成最小 TNN 生命周期闭环：

1. 定义并持久化完整 TNN Graph 的最小字段，表达 TNN 输入输出兼容、承接、版本和替代关系。
2. 保持活动图为完整图的运行时视图，不新增重复的第二套持久图。
3. 分离 `active_tnn` 目标集合与 `loaded_tnn` 实际集合。
4. 用集合差分驱动加载、保持和卸载，显式记录加载失败与资源不足。
5. 修订旧的禁止架构测试：禁止固定 Pipeline、重复图和万能 Graph Manager，但不再禁止完整 TNN Graph 本身。
6. 让 Dock 产物登记到完整图，再由 Core 加载；Dock 不直接决定在场集合。

验收应使用至少两个存在上下游关系的训练 artifact，并验证持久图、目标集合、实际加载集合和运行命名输出相互一致。

## 2. 后续实现顺序

### A. 输入、安全与状态基线

- 让正式入口可选择真实 screen/cursor reader，同时保持默认 Mock 输出。
- 实测 1080p 捕获频率并逐步接近 30fps 目标。
- 加入键盘状态、活动窗口和音频输入所需的最小捕获。
- 建立真实用户接管 hook、Esc 急停和完整状态恢复测试。
- 扩充 world、myself 与 Blackboard 的最小时间语义。

### B. Memory 基础闭环

- 支持图片、音频、轨迹和 state 等非 JSON 大对象。
- 持久化 Event、基础时间边和输入输出承接边。
- 让 STM、MTM 始终引用同一 MemoryID/LTM 权威对象。
- 建立策略、即时动作与延迟结果的跨时间关联测试。

### C. 完整 TNN Graph 与运行生命周期

- 完成当前最高优先级任务。
- 验证高频、低频和事件触发 TNN 并存。
- 加入延迟预算、故障暂停、版本替代和最小回滚。

### D. 本地 LLM 与六大激素

- 接入一个主要本地 LLM。
- 编写实际使用的中文 Prompt，读取 world、myself、Blackboard、Memory 和 TNN 简表。
- 输出 ID-only `active_tnn` 目标集合。
- 从用户反馈、成功、失败、风险、新奇与资源压力更新六大激素。
- 实测 10—20 秒节律映射，防止循环重叠和振荡。

### E. 云端、长期策略与动作

- 建立不阻塞快速路径的云端 LLM 异步调用。
- 让低频策略结果持续约束快速动作 TNN。
- 验证迟到结果不改变已经过去的动作。
- 在人工授权、可急停和 Mock 验证充分后，再做受控真实输出。

### F. Dock 成长证据

- 让 TrainingOrder 的 teacher、延迟约束和评估方式真正生效。
- 从 Memory 和上游 TNN 结果取样，而不只使用手工合成 payload。
- 加入离线评估、延迟硬门槛和必要影子运行。
- 在同一真实任务上做训练前后对照；只有可信改善才记录为新能力。

### G. 睡眠复盘与长期整理

- 在用户触发或长期空闲时复盘成功、失败和延迟反馈。
- 按真实数据规模引入 Event 合并/分离、可逆图压缩和预兆保护。
- 由本地 LLM 判断训练、补训、继续采集或暂不处理。
- 保留输入、安全和急停能力，降低或停止主动输出。

### H. 监视控制与资源

- 补齐主入口监视控制窗口。
- 显示输入、world/myself、激素、Memory、TNN、Dock、模型状态和实际循环频率。
- 资源不足时优先保护输入、状态、安全门和快速反应路径。

## 3. 仍待实验决定

以下问题没有足够证据时不冻结接口：

1. 六大激素的基础浓度、事件 delta、恢复速度和 10—20 秒映射函数。
2. `active_tnn` 第一版除 ID 外是否出现必须使用 binding 的真实场景。
3. Blackboard 条目的最小观测时间、生成时间、有效期和覆盖规则。
4. 长期策略主要驻留在 Blackboard、world 还是 myself。
5. 完整 TNN Graph 边的最小字段、版本替代和回滚方式。
6. 资源不足而无法加载全部 `active_tnn` 时的最小处理办法。
7. 哪些 Blackboard 结果自动进入 Memory，哪些只保留在运行时。
8. 低频长期 TNN 的训练样本如何从跨时间 Memory 关系中提取。
9. 本地 LLM 在何种上下文下请求云端帮助。
10. Memory 合并、分离、稠密二部压缩和序列预兆保护的实际参数。
11. 第一项真实成长任务、teacher 来源和成功指标。
12. 本地 LLM 模型、量化、CPU offload 与服务方式。

决定这些问题时，每轮实验记录：

- 唯一研究问题；
- 固定条件和新变量；
- 输入、模型、输出模式与人工参与；
- 运行命令、日志和指标；
- 本轮没有证明什么；
- 保留或推翻的设计；
- 唯一的下一步。

使用 `EXPERIMENT_RECORD_TEMPLATE.md` 记录，不再新增一次性根目录 Prompt、审计报告或“完成状态”文档。
