# EVE 路线图与未决问题

本文件只保留尚未实现或需要真实外部条件验证的工作。目标语义见
`ARCHITECTURE.md`，当前事实见 `STATUS.md`。

通用纵向闭环已经通过 Fake VLM、Mock Output、合成数据和本地 Dock 训练测试；
红蓝目标成长实验仍暂缓到独立人工验收。合成闭环不得替代真实桌面证据。

## 当前优先级

1. 完成真实桌面会话中的 GUI 冷启动、暂停、恢复、急停和正常停机验收。
2. 用真实 TNN artifact 记录多频率运行、超期、跳过、资源占用和五槽压力。
3. 验证 Catalog、STM 热视图、MTM 工作集和 LTM 持久视图的长期运行分布。
4. 在存在真实训练证据来源时设计通用标签协议；VLM 继续保持通用视觉工具。
5. 验证正常停机的 `world.md` 与 `self.md` 在用户指定 run 目录中可读。
6. 按人工验收顺序验证真实 LLM 主动请求 VLM、真实授权动作、动作后观察、
   自主 materialization、Dock 验收和正式 TNN 加载；全程不读取小游戏内部状态。

## 后续工作

- 提醒：当前 YOLO 识别能力和类别覆盖仍然有限，后续需要单独评估模型、
  数据与阈值；本轮只保证默认启动与运行状态可观测，不解决识别质量。
- 真实睡眠整理：摘要、合并、索引更新、遗忘和可逆证据保护。
- 麦克风听觉；未实现前不显示伪状态。
- 注意力/焦点；未实现前不显示伪位置。
- 训练前后同任务对照、留出评估、延迟门槛和必要的影子验证。
- 用真实任务验证当前 Dock 一次性 QNN 的校准、排序稳定性与 Actor 收益。

## 仍待实验决定

- goodness 的证据校准、置信度与合法候选排序方式。
- `active_tnn` 是否需要除 ID 外的最小 binding。
- Blackboard 条目的时间、有效期和覆盖规则。
- 资源不足时加载 `active_tnn` 子集的最小策略。
- 哪些非动作重要事件也应形成显式 observation bundle；动作执行已固定形成。
- 本地 LLM 何时请求云端帮助。
- 第一项真实成长任务、训练证据来源和成功指标。

## 统一好度闭环之后仍需完成

临时 QNN 的第一版 Dock 生命周期已经实现；此前“是否采用临时 QNN”的待定项由本轮决定取代。后续工作不是增加运行时 QNN，而是：

1. 用真实任务校准 GoodnessRecord 的证据质量、置信度和不同 `value_version` 的可比范围。
2. 验证 `mean` 与 `minimum` 之外的聚合语义；在有真实需求前不增加通用规则引擎。
3. 设计连续动作候选的受限生成实验；当前 QNN 只排序明确提供的已有候选。
4. 用真实 Actor TNN、历史回放和留出任务验证 evaluation/regression goodness，而不是只依赖合成样本。
5. 单独提升 VLM/YOLO 事实质量；当前冻结帧绑定正确不等于识别质量已经充分。
6. 进行真实桌面长时间运行与正常停机验收，包括 `world.md`、`self.md` 和完整调试日志。
7. 校准 after-VLM 失败、视觉无变化和画面迟到时的事实置信度；不得硬编码为成功或失败。
