# Codex与其他代理的使用方式

## 1. 基本分工

用户：

- 决定EVE的核心语义；
- 选择想研究的问题；
- 决定是否开启真实输出。

ChatGPT：

- 整理架构；
- 设计实验；
- 审查Codex结果；
- 识别过度工程和语义偏移；
- 生成下一轮Prompt。

Codex：

- 阅读真实仓库；
- 实现当前一个Phase；
- 运行代码和测试；
- 提供真实日志；
- 更新STATUS。

其他代理：

- 可以做性能实验、独立代码检查、fixture或算法比较；
- 不同时修改同一核心文件。

## 2. Codex任务原则

每次任务应尽量只包括：

- 一个研究问题；
- 一条最小纵向链；
- 明确不做的后续能力；
- 一组真实运行命令；
- 一段日志或指标。

不要使用：

```text
完成整个EVE
实现完整Core
把所有规划都做完
```

作为单次任务。

## 3. 允许Codex自行决定的事情

Codex可以自行决定：

- 小型类和函数命名；
- 局部数据结构；
- 测试fixture；
- 日志辅助函数；
- 不改变架构的代码组织；
- 明显的bug修复。

## 4. 需要用户或架构审查的事情

以下内容不要静默决定：

- 新增顶层模块；
- 恢复Router、Goal Engine或固定多Agent；
- 改变MemoryUnit含义；
- 新建独立TNN Graph对象；
- 改变TNN与LLM关系；
- 真实输出默认开启；
- 删除历史Memory或模型；
- 把Mock结果描述为真实成长；
- 修改长期策略和激素的核心语义。

## 5. 简化交接

每轮Codex只需报告：

1. 改了什么；
2. 运行了什么；
3. 看到什么；
4. 哪些是假数据；
5. 哪些是真实输入/模型；
6. 什么仍然没有实现；
7. 下一步只建议一件事。

不要求填写正式项目式的大型handoff表。
