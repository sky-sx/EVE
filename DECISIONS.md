# EVE已确认决定

## 总体

1. EVE是个人数字生命研究装置，不是商业产品或通用框架。
2. 遵循“非必须不设计”。
3. 不建立固定中央Router、Goal Engine、竞价市场或固定多Agent角色体系。
4. 正式代码根是`eve/`；`src/eve/`只作参考。
5. 一次实验尽量只增加一个主要变量。
6. Mock、真实输入、真实任务和成长结果必须分开描述。
7. Git、虚拟环境、完整CI和发布流程是可选工具，不是开发门禁。

## Input与Output

1. 快速输入路径不能等待LLM、Memory整理或Dock。
2. 输出默认`disabled`或`mock`。
3. 所有真实动作经过Safegate。
4. Esc具有最高停止优先级。
5. 人类鼠标或键盘活动后，v1键鼠输出冻结5秒；未来可根据个人使用体验调整。
6. 真实输出只在受控任务和人工授权下开启。

## Memory

1. `MemoryUnit = MemoryID + payload`。
2. LTM保存真实payload。
3. Catalog完成MemoryID到LTM对象的映射。
4. STM和MTM是MemoryID集合与可丢弃缓存，不复制权威payload。
5. 尚未提交保存的连续输入属于buffer。
6. Event位于MemoryUnit之上。
7. 复杂merge、redirect、压缩和遗忘在最小Memory及成长闭环跑通后再实现。

## TNN

1. TNN descriptor是输入、输出、频率和时间角色的事实源。
2. 潜在上下游通过SourceRef表达，不单独维护图边。
3. TNN Graph不是独立实体。
4. `active_tnn`是期望在场名单；`loaded_tnn`是实际加载名单。
5. 第一版不实现通用binding或图修改语言。
6. 动作TNN主要输出动作/倾向和执行程度，不强制使用Q值。
7. QNN若使用，只存在于Dock训练期，不进入运行时TNN列表。
8. LLM推理延迟不得被错误蒸馏为目标TNN的动作时间。

## LLM、激素与Dock

1. 主要使用一个本地LLM。
2. 云端LLM是异步教师或增强节点。
3. 激素影响Prompt、倾向和节律，不直接发动作。
4. Dock只执行具体训练订单，不决定运行时加载。
5. 第一次成长证明可以使用人类、规则或现有模型作为教师，不必先实现本地LLM。
6. 只有同一任务训练前后出现可信改善，才称为长出了一项能力。

## 开发方式

1. Phase是实验顺序，不是正式审批。
2. 每轮保留足够日志，能够判断失败在哪一层。
3. 可在大修改前复制工程目录或使用Git；不强制特定方式。
4. 不为了文档完整预先冻结接口。
5. 不因旧测试存在而恢复旧架构。
