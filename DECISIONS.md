# EVE 已确认决定

本文件只记录已经确认的长期决定。目标细节见 `ARCHITECTURE.md`，当前代码事实见 `STATUS.md`，仍待实验决定的问题见 `ROADMAP.md`。

## 定位与边界

1. EVE 是个人数字生命研究装置，不是商业产品、通用 Agent 框架或企业平台。
2. 遵循“非必须不设计”；能力应从运行、记忆、反馈与训练中形成。
3. 正式代码根为 `eve/`；`src/` 与 `reference/` 只作参考。
4. 除主入口外，逻辑架构只有 Input、Output、Memory、Core、Dock 五部分。
5. 不建立固定中央 Router、独立大型 Goal Engine、实时竞价市场或固定多 Agent 角色体系。
6. 目标架构和当前实现必须分开描述。

## 输入、输出与安全

1. Capture 只捕获，Buffer 只维护近期 state；快速输入路径不能等待 LLM、Memory 整理或 Dock。
2. Output 只执行已经通过 Core 权限、急停、接管与有效期边界的动作，并在执行前复检。
3. 默认输出为 `disabled` 或 `mock`；真实输出只能在受控任务与人工授权下开启。
4. Esc 急停和用户主动接管具有最高优先级。
5. 人类键鼠活动后的 5 秒冻结是当前 v1 默认值，不是永久架构常量。
6. 动作结果必须区分真实执行、模拟、失败和阻断，并保留最小时间语义。
7. 动作由执行环拉取当前有效结果，不采用实时竞价。

## 运行时状态

1. world、myself、Blackboard 与近期 state 在内存中维护。
2. Blackboard 是可覆盖、可过期的运行时交换区，不是长期 Memory。
3. 状态文件只用于启动、停机、睡眠、手动保存和异常恢复，不做高频状态本体。
4. 长期策略提前写入运行时状态，由快速 TNN 和即时感知共同读取。

## Memory

1. 最小语义是 `MemoryUnit = MemoryID + 对应数据`。
2. LTM 保存多模态权威数据；STM 与 MTM 是 MemoryID 工作集及可丢弃缓存，不复制权威 payload。
3. Event 位于 MemoryUnit 之上，索引先返回 MemoryID 或 EventID。
4. 第一版以时间边为基础，只按真实需要增加承接、因果候选、任务、相似和同源边。
5. 复杂整理必须保留原始证据并可逆；连续序列必须保护预兆信息。
6. 策略、即时动作与延迟结果应分别保存，再建立跨时间关系。
7. TNN 权重与普通 MemoryUnit 数据分开保存，但训练来源、版本和经历通过 ID 关联。
8. Catalog 保存全部 MemoryUnit；STM 是有界热视图，MTM 是显式工作集，LTM 是显式持久语义集，三者可以重叠。STM 淘汰不删除 Catalog 数据，也不自动改变 MTM/LTM。

## TNN 与图

1. TNN 只表示 Tiny Neural Network；LLM、VLM、基础模型与 Memory 检索不是 TNN。
2. 各类节点共享的是输入、输出、延迟、成本和上下游结果等运行语义。
3. 完整 TNN Graph 是必须持久化的能力图，核心对象是可训练 TNN 及其潜在承接、版本和替代关系。
4. 活动图只是完整 TNN Graph 当前已加载子集与相关运行节点的视图，不是第二套独立持久图。
5. `active_tnn` 是本地 LLM 声明的期望在场集合；`loaded_tnn` 是实际加载集合，两者不能混为一个状态。
6. 第一版 `active_tnn` 只列 TNN ID；出现真实动态绑定用例后才增加最小 binding。
7. TNN 输出不强制解释为 Q 值，不预定义具体任务能力。
8. 低频长期 TNN 的价值不能只按自身调用次数判断。

## LLM、好度与 Dock

1. 主要使用一个本地 LLM 完成不同时间尺度的通用思考，不预拆为多个固定 Agent。
2. 云端 LLM 是异步增强或教师节点；迟到结果只能影响仍有效的问题和未来行为。
3. self 只保留统一 goodness 软价值及证据；行为倾向是数值而不是权限，权限与急停仍是硬边界。
4. 本地 LLM 输出 `active_tnn` 完整目标集合，不进行每帧调度。
5. Dock 只执行具体训练方案，不决定运行时加载，也不拥有固定训练触发器。
6. 训练证据可以来自人类、环境、现有模型、本地或云端 LLM；VLM 本身是通用视觉工具，不固定为教师角色。
7. 只有同一真实任务在训练前后出现可信改善，才能称为长出了一项能力。
8. LLM 的 training proposal 与可执行 TrainingOrder 分离；Dock 只训练明确提供的 concrete `model.py` 或模型 MemoryID。

## 开发与证据

1. Phase 表示控制变量的实验顺序，不是形式化审批门禁。
2. 一次实验尽量只增加一个主要变量。
3. Mock、合成模型、真实输入、真实输出和真实成长必须分别陈述。
4. 测试用于保护因果链与安全边界；通过测试不自动等于目标能力成立。
5. 不因旧代码或旧测试存在而恢复已经否定的架构。
6. 任何接口只在真实消费者出现时冻结，不为了文档完整提前扩张。
