# EVE 完整目标架构

## 2026-07-28 已落地的交互运行时边界

`main.py` 现在直接承载 PySide6 八页监视与控制窗口。GUI 只读 Buffer/Core/Memory 发布状态并向 Core 提交有界请求；它不直接访问 Capture、模型、Memory 文件或 Output。

```text
GUI / main.py
  ↓ 冷启动、暂停、恢复、急停、关闭、用户请求
InputBuffer ↔ Capture 独立进程（屏幕/光标/键盘活动/活动窗口）
  ↓
Core（world / myself / Blackboard / LLM / VLM / cloud / TNN≤5）
  ↔ Memory（异步写入 / Event / 检索 / 整理）
  ↓ 动作候选
Safegate（原子权限 / 急停 / 接管 / 有效期 / 最低范围）
  ↓
Output（鼠标 / 键盘 / Unicode 文本 / TTS）
  ↓
Core / Memory
```

本地生成式模型的固定边界是 CUDA 4-bit NF4，加载后必须检查模型真实量化状态；没有模型权重或量化失败时节点进入可见错误状态。Input 不包含麦克风、录音、音量或语音识别；TTS 仅属于 Output。

运行时只使用 Core 现有 `loaded_tnn` 和 SourceRef 关系呈现五个 TNN 槽位，不建立第二套 TNNGraph、Manager 或 Router。

本文是 `EVE完整架构描述.txt` 的结构化工程表达，描述目标架构，不代表所有能力已经实现。当前实现状态只见 `STATUS.md`。若本文与完整架构原文冲突，以原文和用户最新明确决定为准。

## 1. 总体原则

EVE 是运行在电脑环境中的可成长数字生命体原型。它通过持续多模态输入形成对世界和自身的认识，通过本地 LLM、云端 LLM、基础模型和多个 TNN 完成思考与动作，通过 Memory 保存结果及关联，通过 Dock 把慢速、高成本或重复计算训练、压缩为新的 TNN。

EVE 不采用固定任务流水线，也不建立一个预先枚举所有任务和路径的中央 Router。系统只保留稳定的运行边界，具体能力、节点组合、长期策略和训练对象应从经历、反馈与实验中逐渐形成。

顶层逻辑结构为：

```text
main.py
input/
output/
memory/
core/
dock/
```

辅助文件可以承载数据结构或局部实现，但不得形成第六个拥有独立架构职责的顶层子系统。

## 2. 完整数据流

```text
连续多模态输入
→ buffer 形成带统一时间语义的 state
→ 基础模型、已加载 TNN、LLM 和 Memory 检索节点读取所需结果
→ 节点结果写入 blackboard、world、myself 或 Memory 候选
→ 快速 TNN 同时读取即时感知与既有长期策略
→ 产生带有效期的动作候选
→ 动作执行环拉取仍然有效的结果
→ Safegate 检查权限、急停、用户接管和最低安全边界
→ output 执行并返回真实结果
→ 动作后的环境变化继续被 input 捕获
→ 各阶段结果作为独立 MemoryUnit 保存并通过边与 Event 组织
→ 经历更新六大激素并影响下一轮本地 LLM 节律
→ 本地 LLM 更新理解、长期策略和 active_tnn 目标集合
→ Dock 在存在具体训练方案时从 Memory 取样、训练和评估 TNN
→ 新 TNN 登记到完整 TNN Graph，之后由本地 LLM 决定何时加载
```

实时捕获和快速动作路径不能等待慢速 LLM、Memory 整理、云端调用或 Dock 训练。

## 3. main.py

`main.py` 是唯一正式主入口，负责：

- 加载配置；
- 初始化监视与控制界面；
- 初始化 input、output、Memory、Core、Safegate、基础模型和 Dock；
- 加载 world、myself、blackboard 停机快照；
- 读取可用 TNN 的简化列表；
- 绑定冷启动、常规停机和 Esc 急停；
- 启动、停止各运行循环；
- 常规停机时等待重要写入并保存恢复状态；
- 异常停止时在不延误动作停止的前提下尽可能写 recovery 快照。

监视与控制界面应能逐步覆盖输入画面与分析、音频状态、文本交互与自主思想、world、资源与循环频率、冷启动与急停、Memory 状态与睡眠整理、输出权限与行为倾向、模型与 API 状态、六大激素、TNN 生命周期以及 Dock 训练状态。

界面是观察与控制入口，不是新的业务子系统。早期可以使用最小界面，但必须诚实标注缺失能力。

## 4. Input

### 4.1 capture.py

只负责连续捕获原始输入：

- 屏幕图像，目标能力为 1080p、30fps；
- 麦克风音频；
- 鼠标光标位置；
- 键盘状态；
- 当前活动窗口；
- 对应统一时间戳。

Capture 不进行高级理解、任务判断、长期保存或动作决策。不同输入源可使用不同频率，但必须能够对齐到共同时间范围。

### 4.2 buffer.py

维护最近一段时间的输入缓冲，对其他模块统一提供 `state`：

- 默认读取最近约 1 秒完整输入；
- 读取各类 `latest`；
- 按指定时间范围读取；
- 保留统一、单调的时间语义。

Buffer 中的数据尚不是 Memory。是否保存某一帧、音频、轨迹或 state 快照，由使用结果的模块或后续整理过程决定。

## 5. Output 与 Safegate

`keyboard.py`、`mouse.py` 和 `speak.py` 把统一 JSON 指令转换为键盘、鼠标和语音输出。Output 只执行已经通过 Safegate 的动作，不自行理解任务或选择动作。

每次输出都应产生结果，至少表达：

- 原动作指令；
- 开始和完成时间；
- 是否真实执行、模拟、失败或被阻断；
- 必要执行反馈；
- 与原观测和期望作用时刻相关的最小时间语义。

Safegate 只负责最低安全边界：

- EVE 是否得到相应控制权限；
- Esc 或急停是否触发；
- 用户是否主动接管；
- 动作是否过期或已不适用；
- 动作是否超出明确允许范围；
- 是否存在必须阻断的状态。

Safegate 不重新做高级任务判断。用户操作键鼠后 EVE 必须立即让位；当前实现可使用 5 秒冻结作为 v1 默认值，但它不是永久架构常量。

动作输出不采用实时竞价。执行环在需要动作时拉取 Blackboard 中当前仍有效的结果；迟到的 LLM 结果只能影响尚未解决的问题和未来行为，不能改写已经过去的动作时刻。

## 6. 运行时状态

EVE 在内存中维护 `world`、`myself`、`blackboard` 和近期输入 `state`。

### world

保存 EVE 对外部世界的当前理解，包括大场景、细分场景、活动窗口、可见对象、识别文本、环境变化、任务相关状态、不确定信息和最后更新时间。

### myself

保存 EVE 对自身的当前理解，包括注意位置、控制权限、当前任务、自主思想、资源、模型状态、已加载 TNN、可用 TNN 简表、六大激素、行为倾向和必要内部状态。

### blackboard

是 TNN、LLM、VLM、Memory 检索及其他运行节点交换当前结果的共享区域。它可容纳感知结果、对象状态、预测、动作候选、长期策略、临时思考、待解决问题、候选状态更新、冲突结果和待教师确认信息。

Blackboard 不是长期记忆。条目可以覆盖、过期或清理；仍有长期价值的结果必须另存为 MemoryUnit。

`world.md`、`self.md` 和 `blackboard.md` 若存在，只用于启动、停机、睡眠复盘、手动保存和异常恢复时的可读快照，不能作为高频运行时状态本体。具体快照格式可以是 JSON 或其他可靠格式，不以 Markdown 扩展名作为架构要求。

## 7. Memory

### 7.1 MemoryUnit、工作集与 LTM

Memory 保存各部分得到了什么，以及这些结果之间的关联。最小单元为：

```text
MemoryUnit = MemoryID + 对应数据
```

数据可以是文本、图片、音频、视频片段、帧序列、轨迹、state 快照、基础模型输出、TNN 输出、LLM 输出、动作结果或其他模块结果。

MemoryUnit 不强制携带决策、评价、reward、置信度、任务归属和完整上下文。需要表达这些信息时，将其保存为其他结果，再通过边或 Event 关联，避免最小单元持续膨胀。

- STM：近期连续产生或使用的 MemoryID 工作集，服务当前运行和近期事件形成。
- MTM：当前任务、当天事件、待整理结果、待确认关系和仍可能影响当前运行的经历。
- LTM：多模态权威数据存储。

STM 和 MTM 不复制一套权威 payload；索引先返回 MemoryID 或 EventID，再按需读取真实数据。

### 7.2 Event、索引与关联

Event 位于 MemoryUnit 之上，把多个 MemoryUnit 组织为一次经历。它可以包含事件 ID、MemoryID 集合、时间范围、摘要、标签和事件间关系，但这些不是每个 MemoryUnit 的固定字段。

第一版关联以时间边为基础，按真实需要逐步增加：

- 输入输出承接边；
- 因果候选边；
- 任务边；
- 内容相似边；
- 同源边；
- 经过实验确认有价值的其他边。

不同检索索引可以形成各自的图或关联结构。边类型不得在没有用例时无限扩张。

### 7.3 整理与保护

当 A、B 两类节点间出现近乎完全图连接时，可以进行可逆的稠密二部关联压缩：分别选择两侧中心节点，以交叉中心连接代替大部分全连接；默认压缩后最短路径上限为 3，超限节点退回原始连接，原证据必须可恢复。

连续视觉和其他序列整理必须保护预兆信息，例如目标出现前的边缘、尖角或微小变化。合并节点时要受最长路径、最长边长度或等价保护参数约束。

长期策略可能很晚才得到反馈。Memory 必须允许分别保存当时策略、随后即时动作和最终结果，并在复盘时建立跨时间关联。低频长期 TNN 的价值不能只按运行次数判断。

TNN 权重和最小描述保存在 `TNNweights`，与普通 MemoryUnit 数据分开；训练来源、版本替代和相关经历通过 ID 与 Memory 关联。

## 8. Core、TNN 与统一节点语义

Core 是运行时内核，负责状态、运行循环、已加载 TNN、Blackboard 交换、LLM 调用、模型资源、六大激素、资源约束和动作安全。Core 不训练 TNN，也不是中央任务 Router。

TNN 始终是 Tiny Neural Network。一个 TNN 拥有自己的 ID、版本、输入描述、命名输出、结构、权重、运行频率或触发方式、时间范围和延迟约束。

本地 LLM、云端 LLM、VLM、YOLO、Memory 检索与 Output 可以和 TNN 共享节点运行语义：都有输入、输出、延迟、成本、运行状态和上下游结果。但 LLM 与基础模型不是 Tiny Neural Network，不得命名为 TNN。

### 8.1 完整 TNN Graph

完整 TNN Graph 是持久化能力图，保存可训练 TNN 之间可能的逻辑承接关系，包括：

- 哪些输入类型可被某 TNN 读取；
- 哪些 TNN 可以消费其他 TNN 的命名输出；
- 哪些 TNN 产生中间结果；
- 哪些 TNN 可以形成动作或状态更新；
- TNN 的替代、版本和联合训练关系。

它不是固定串联 Pipeline。一个 TNN 可有多个上游和下游，不同任务可加载不同子集。边表达数据语义承接，不要求写成 Python 硬调用链。

LLM、VLM、Memory 检索和 Output 可在统一运行图中与 TNN 承接，但完整 TNN Graph 的核心持久化对象仍是可训练 TNN 及其关系。实现应使用能够满足这一语义的最小数据结构，不额外制造重复图、全局万能调度器或无消费者的 Graph 元数据。

### 8.2 活动图、active_tnn 与 loaded_tnn

活动图不是第二套持久化图。它是完整 TNN Graph 中当前已加载、已注册运行的 TNN 子集，以及当前与其交换结果的其他计算节点。

- `active_tnn`：本地 LLM 声明当前应该在场的完整 TNN ID 目标集合。
- `loaded_tnn`：运行时实际成功加载的 TNN 集合。

Core 对两者做集合差分：目标新增项加载，目标移除项卸载，共同项保持运行。加载包括读取权重、初始化状态、注册输入监听和运行频率；卸载包括停止循环、取消监听和释放资源，但不删除权重、描述、历史结果或完整图关系。

第一版 `active_tnn` 默认只包含 ID。只有真实出现同一 TNN 必须动态切换输入绑定的用例时，才增加最小 `binding`。

## 9. 本地 LLM、云端 LLM 与长期策略

EVE 主要使用一个本地 LLM 进行较慢的通用思考，不预拆为路由、反思、Memory、目标等固定 Agent。

本地 LLM 每轮读取必要的 world、myself、Blackboard、当前任务、六大激素及趋势、已加载 TNN、可用 TNN 简表和 Memory 检索结果，并可以：

- 更新对 world 和 myself 的理解；
- 形成自主思想与长期策略；
- 声明下一阶段完整 `active_tnn` 集合；
- 记录待解决问题；
- 判断是否需要云端 LLM；
- 判断是否存在值得训练、补训或继续采集数据的能力。

如果本地能力不足，可以异步提交最小上下文包给云端 LLM。云端结果是可能迟到的增强或教师结果，不拥有无条件覆盖其他结果的最高权限。

长期策略由本地 LLM 或低频长期 TNN 提前生成，压缩后保存在 Blackboard、world 或 myself 中。快速动作 TNN 同时读取即时感知和当前策略，因此可以在毫秒级动作中遵守分钟或小时尺度约束，而不等待新的 LLM 推理。

## 10. 六大激素与内在节律

六大激素是 `myself` 中持续变化的运行状态：

- dopamine：成功、表扬、有效路径与持续投入；
- serotonin：长期稳定、策略保持与减少无意义切换；
- norepinephrine：突发变化、警觉、高优先级与重思考速度；
- oxytocin：用户反馈、意图与互动关系的重要程度；
- cortisol：失败、风险、阻断、冲突与资源压力；
- acetylcholine：注意、学习、新奇与训练数据关注。

它们从上一轮浓度继续变化：

```text
新浓度 = 旧浓度 + 当前事件变化 + 长期状态偏移 + 自然恢复或衰减
```

激素影响 LLM 提示上下文、行为倾向和下一轮本地 LLM 间隔，但不能直接产生动作或绕过 Safegate。大循环目标间隔初始限制在 10—20 秒：稳定时趋近 20 秒，新奇、风险、冲突或学习需求上升时趋近 10 秒。具体映射参数必须通过运行实验确定。

## 11. Core 运行循环

逻辑上至少存在以下职责；实现可以在不混淆职责的前提下合并少量线程或函数：

1. 实时捕获：按各输入源频率持续采样，不等待慢任务。
2. 缓冲更新：对齐时间戳，维护 latest 与最近约 1 秒 state。
3. 基础感知：按模型延迟读取 state，发布独立结果。
4. TNN 直接反应：按各自频率或事件运行，发布命名输出。
5. 本地 LLM 大循环：更新理解、策略、问题、训练考虑和 `active_tnn`。
6. 激素与节律：汇总经历、更新浓度并计算下一轮间隔。
7. 云端 LLM 异步调用：不阻塞快速路径，迟到结果写回 Blackboard。
8. TNN 生命周期：集合差分、加载、卸载、注册、故障与资源处理。
9. 动作执行：拉取有效动作、通过 Safegate、执行并记录结果。
10. Memory 写入与整理：保存独立结果、建立基础边、形成 Event 和维护工作集。
11. 睡眠复盘：在用户触发或长期空闲时整理经历、延迟反馈和训练候选。
12. 资源监控：观察 CPU、GPU、内存、显存、模型与循环频率。
13. 状态持久化：在停机、睡眠、手动保存、重要变化和恢复点保存快照。

睡眠复盘期间降低或停止主动键鼠输出，但必须保留必要输入、安全与急停能力。资源不足时优先保护输入、基础状态、Safegate 和已加载快速反应路径。

## 12. Dock

Dock 主要包含 `trainer.py` 和 `tinynn.py`。它负责定义、训练、评估和生成 TNN，不决定当前 `active_tnn`，也不维护活动图。

训练订单至少表达：

- 创建新 TNN 或补训已有 TNN；
- 训练目标；
- 输入与命名输出；
- 可使用的 Memory 数据与上游 TNN 结果；
- teacher 来源；
- 结构、延迟和资源约束；
- 离线、延迟与必要影子评估方式。

Teacher 可以是本地 LLM、云端 LLM、VLM、人类、环境反馈或已有 TNN。临时标签与原始 MemoryUnit 分开保存并通过 ID 和边关联。

`TinyNN` 提供高自由度结构能力，能够表达卷积、池化、MLP、层数、宽度、连接和输出等真实结构，不强制把输出解释为 Q 值。不同 TNN 有不同延迟预算；反射级目标可约为 30ms，语言或长期判断可以更慢。超过任务硬延迟约束的模型不能只凭准确率通过部署评估。

训练完成后，Dock 生成权重、结构、描述、训练来源和评估结果，登记到 `TNNweights`、Memory 与完整 TNN Graph。是否立即加载由之后的本地 LLM 大循环决定。

Dock 不拥有独立硬编码训练触发器。Core 只提供空闲资源等客观状态；本地 LLM 在有具体价值时提出训练、补训、继续采集、请求教师或暂不训练。

## 13. QNN 动作评价

QNN 是一个由 Dock 训练、由 Core 加载的 TinyNN critic，不是固定规则、
Safegate 或普通分类器的别名。v1 输入为当前屏幕状态和一个候选鼠标动作，
输出范围 `[-1, 1]` 的 `q_value`，表达该状态下执行该动作的期望环境奖励。

训练数据来自完整 Experience：

```text
screen state + action + environment reward
```

命中通常产生正奖励，未命中产生负奖励；训练集和独立留出集不得混用。
QNN artifact 与动作 TNN artifact 一样保存模型、权重、结构、数据来源和
评价指标。

运行时顺序为：

```text
多个 TNN 动作候选
→ QNN 对同轮候选逐一评分
→ 选择最高且超过最低分数的候选
→ Safegate
→ Output
→ 环境反馈写回 Experience
```

QNN 不得绕过 Safegate；QNN 不可用或评价出错时，已启用 QNN 的鼠标候选
应停止执行并留下错误记录。非鼠标动作不由 QNN v1 评分，仍走原有
Safegate。

Dock 可以使用已加载 QNN 对候选动作 TNN 计算独立 `fitness_data` 上的
平均 fitness，并要求绝对最低值或相对已加载旧版本的最小提升 margin。
未通过 QNN fitness 的候选只登记为拒绝结果，不替换当前模型。

## 14. 开发顺序

1. 主入口、Input、Output、30fps 目标、最近约 1 秒 state、统一输出和 Esc 急停。
2. world、myself、Blackboard 与停机快照，运行时不高频写文档。
3. MemoryUnit、STM、MTM、LTM、基础时间边、Event 和多模态保存。
4. 单 TNN 的描述、权重加载、输入读取、Blackboard 输出、频率运行和卸载。
5. 完整 TNN Graph 与声明式 `active_tnn` 生命周期。
6. 六大激素、本地 LLM 大循环、10—20 秒动态节律和对应中文 Prompt。
7. 云端 LLM 异步调用。
8. Dock 最小训练闭环、延迟评估和重新登记。
9. 长期策略影响即时动作，以及激素影响节律和在场 TNN 的验证。
10. 睡眠复盘、延迟反馈、图压缩和机会式训练考虑。

每阶段同步准备 Prompt、最小任务、输入构造、输出捕获和日志分析。不得在后续模块先写成“完整”后，再用 Mock 或静态结构倒推能力成立。

## 15. 明确不采用的架构

在没有实验需求前，不新增：

- 预先枚举任务的中央 Router；
- 独立大型 Goal Engine；
- 实时动作竞价市场；
- 固定多 Agent 角色体系；
- 让 LLM 每帧选择 TNN 的调度方式；
- 把 LLM、VLM 重新命名为 TNN；
- 与完整 TNN Graph 重复的第二套持久图；
- 把运行时 Blackboard 当长期 Memory；
- 高频读写 Markdown 状态文件；
- 没有真实消费者的大量字段、Manager 和抽象层。
