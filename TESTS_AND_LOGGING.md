# EVE测试与实验记录原则

## 1. 证据类型

不再使用正式成熟度审批，只区分以下结果：

### 结构验证

文件、语法、导入和数据结构能工作。

### 模拟验证

链路使用Fake或Mock运行，证明接口能连通。

### 真实输入验证

使用真实屏幕、光标、音频等，但动作可继续Mock。

### 真实任务验证

在受控小游戏或真实任务中运行，记录成功和失败。

### 成长验证

训练新TNN前后在相同任务条件下对比，证明行为改善。

描述结果时必须说清属于哪一种。

## 2. 测试的目的

测试主要保护：

- Safegate和急停；
- 默认无真实输出；
- 时间语义；
- MemoryID映射；
- TNN加载、卸载和输出；
- 实验条件没有无意改变；
- 旧架构没有被恢复。

不追求高覆盖率数字。

## 3. 日志最小要求

每次实验至少记录：

- run_id；
- 时间；
-实验名称；
- 配置摘要；
- 输入来源；
- 使用的模型/TNN及版本；
- 关键中间结果；
- ActionCandidate；
- Safegate结果；
- OutputResult；
- 成功/失败；
- 延迟；
- 异常；
- 人工干预。

推荐JSONL，便于逐行追加和事后分析。

## 4. 时间语义

动作和训练数据必须尽量记录：

- 观测时间；
- 结果生成时间；
- 动作开始时间；
- 动作完成时间；
- 标签参考的MemoryID；
- action horizon；
- 结果是否仍有效。

尤其不能把LLM三秒后产生的动作，直接当作三秒前画面的即时动作标签。

## 5. 安全实验原则

真实输出实验必须：

- 在独立小游戏或受控窗口；
- 由用户显式开启；
- Esc立即停止；
- 人类接管优先；
- 有最大运行时间；
- 有动作范围限制；
- 有日志；
- 先用Mock跑通相同链路。

## 6. Phase 1测试重点

- 默认disabled；
- mock不调用真实API；
- emergency stop；
- 权限；
- 五秒人类接管冻结；
- 过期动作；
- 日志完整；
- 无后台线程泄漏。

## 7. Phase 2测试重点

- 屏幕FPS；
- 时间戳单调；
- buffer边界；
- latest/range；
- 光标时间对齐；
- 安全停止；
- 内存不持续增长。

## 8. Memory测试重点

- MemoryID能定位LTM对象；
- STM/MTM只存ID；
- 同一payload不被复制成三份；
- Event不覆盖底层数据；
- 时间索引能找回结果。

## 9. TNN测试重点

- descriptor与命名输出一致；
- SourceRef可读取；
- 输入缺失处理；
- 频率；
- TTL；
- 推理延迟；
- 加载卸载；
- 不需要独立Graph对象。

## 10. 成长实验对比

固定：

- 游戏版本；
- seed范围；
-分辨率；
- 硬件；
- 输入模式；
- 输出授权；
- 测试轮数；
- 指标计算。

对比：

- 无目标TNN；
- 有目标TNN；
- 禁用目标TNN回滚。

否则不能判断改善来自训练还是测试条件变化。
