[project]
name = "eve-minimal-reflex-kernel"
version = "0.1.0"
description = "Synthetic-only EVE realtime reflex kernel prototype."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
